trigger:
  branches:
    include:
      - main

pr: none   # don't trigger on PRs

variables:
  - group: app-secrets            # contains DATABASE_URL and ARM secrets
    buildConfiguration: 'production'

pool:
  vmImage: 'ubuntu-latest'

stages:
- stage: Build
  displayName: Build and Package Next.js App
  jobs:
  - job: Build
    steps:
    - task: NodeTool@0
      inputs:
        versionSpec: '20.x'
      displayName: 'Install Node.js 20'

    - script: npm ci
      displayName: 'Install dependencies'

    - script: npm run build
      displayName: 'Build Next.js app'

    # Package full app for deployment
    - task: ArchiveFiles@2
      inputs:
        rootFolderOrFile: '$(System.DefaultWorkingDirectory)'
        includeRootFolder: false
        archiveType: 'zip'
        archiveFile: '$(Build.ArtifactStagingDirectory)/app.zip'
        replaceExistingArchive: true
      displayName: 'Archive app into zip'

    - publish: $(Build.ArtifactStagingDirectory)/app.zip
      artifact: drop

- stage: Deploy
  displayName: Deploy to Azure App Service
  dependsOn: Build
  jobs:
  - deployment: DeployWeb
    environment: 'production'   # optional: approvals can be added here
    strategy:
      runOnce:
        deploy:
          steps:
          - download: current
            artifact: drop

          # Deploy the zip to Azure App Service (Linux)
          - task: AzureWebApp@1
            displayName: 'Deploy to Azure Web App (Linux)'
            inputs:
              azureSubscription: 'lola-service-connection'   # your service connection
              appType: 'webAppLinux'
              appName: 'vercel-demo-app'                     # your App Service name
              package: '$(Pipeline.Workspace)/drop/app.zip'
              appSettings: |
                -Name "DATABASE_URL" -Value "$(DATABASE_URL)"
